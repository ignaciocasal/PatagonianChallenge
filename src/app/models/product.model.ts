export interface Product {
  number: string;
  name: string;
  date: string;
  sku: number;
  weight: string;
  height: string;
  width: string;
  origin: string;
  minimum: number;
  delay: string;
}
